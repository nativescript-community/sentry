android {
 defaultConfig {  
    compileSdkVersion 31
    multiDexEnabled true
    minSdkVersion 18
    targetSdkVersion 31
    versionCode 104
    versionName "2.0"
    ndk {
      abiFilters.clear()
    }
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
